import { StyleSheet } from "react-native";

function Card(props) {
  return <Card style={styles.card} title="" subTitle="" image={require("")} />;
}

const styles = StyleSheet.create({
  card: {
    backgroundColor: "#fff",
  },
});

export default Card;
